sap.ui.define(["sap/ui/core/UIComponent"], function (UIComponent) {
  "use strict";

  return UIComponent.extend("monitoringis.shell.Component", {
    metadata: {
      manifest: "json"
    }
  });
});
